nextflow_workflow {

    name "Test Workflow AMR_PROFILING"
    script "subworkflows/amr_profiling/main.nf"
    workflow "AMR_PROFILING"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
                //reference_base = "/work_syn/shared/references"
                //reference_version = "1.1"
            }
            workflow {
                """
                // define inputs of the workflow here. Example:
                input[0] = Channel.of( [ [sample_id:'Ecoli', taxon: 'Escherichia coli'], file("${baseDir}/tests/assets/Ecoli.fasta")])
                input[1] = file("${params.reference_base}/gabi/${params.reference_version}/amrfinder/latest")
                input[2] = Channel.of(['resfinder'])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }

}
